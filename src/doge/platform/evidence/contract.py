"""Public Knowledge & Evidence bounded-context contract.

This context owns documents, pages, chunks, evidence chunks, retrieval, claims,
citations, provenance, run summaries, and eval read models. Current exports are
facade-compatible with existing `doge.application.services` and `doge.core`
objects while physical moves remain story-gated.
"""
