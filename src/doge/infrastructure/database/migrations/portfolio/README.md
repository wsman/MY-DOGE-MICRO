# Portfolio Migrations

Portfolio SQLite migrations are registered in
`src/doge/infrastructure/database/migration_runner.py`.

This source-local directory is the ownership marker for portfolio and holding
migrations.
