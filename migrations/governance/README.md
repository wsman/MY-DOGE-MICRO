# Governance Migrations

Governance SQLite migrations are registered in
`src/doge/infrastructure/database/migration_runner.py`.

This directory is the ownership marker for ACL, audit, approval-actor, and
tenant-governance migrations.
