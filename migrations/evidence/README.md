# Evidence Migrations

Evidence and document SQLite migrations are registered in
`src/doge/infrastructure/database/migration_runner.py`.

This directory is the ownership marker for document, page, chunk, evidence,
embedding, and vector-store migrations.
