# Workspace Migrations

Workspace SQLite migrations are registered in
`src/doge/infrastructure/database/migration_runner.py`.

This directory is the ownership marker for workspace, project, research-case,
workflow-template, execution, asset, and decision migrations.
