# SSO / OIDC Roadmap

- Add OIDC login through the enterprise identity provider.
- Map identity claims to tenant, project, role, and approval authority.
- Reject all non-loopback deployments until auth and CORS allow-listing are enabled.
- Audit every approval and artifact publication with user identity.
