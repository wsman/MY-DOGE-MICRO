# Multi-tenant Roadmap

- Add tenant_id and project_id to every run, artifact, approval, document, and trace record.
- Enforce row-level access in repositories.
- Put tenant budget and token ceilings in the application gateway/runtime, not in the model provider.
- Add tenant-scoped eval dashboards and replay controls.
