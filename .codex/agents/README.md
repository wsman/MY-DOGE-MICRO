# Codex Agents

This directory is reserved for Codex adapter agent definitions. Shared agent
source material remains under `.claude/agents/` unless and until a Codex-specific
TOML agent definition is required.
