# Codex Adapter

`.agents/` contains Codex-readable skill copies. `.codex/` contains Codex hook
configuration and hook script mirrors.

Codex adapter assets must use relative paths such as `.codex/hooks/...`; do not
record machine-specific absolute paths. Canonical CDD workflow, template,
standards, skill testing, and documentation assets remain outside this adapter.
